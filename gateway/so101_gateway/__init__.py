"""Шлюз управления рукой SO-101.

Реализует контракт из ``docs/api.md`` (версия протокола 1). Пакет работает и
без ROS: ``rclpy`` импортируется только внутри ``backends/ros2.py``, поэтому
шлюз запускается на macOS с ``SimBackend``.
"""

__version__ = "0.1.0"
PROTOCOL_VERSION = 1
